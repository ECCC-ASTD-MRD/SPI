#----- Definitions
export BASE_ARCH=`uname -s`_`uname -m | tr _ -`
#echo "(INFO) Architecture detected: ${BASE_ARCH}"

export TCL_VERSION=8.6

export SPI_MAJOR=7
export SPI_MINOR=6
export SPI_PATCHLEVEL=0
export SPI_VERSION=${SPI_MAJOR}.${SPI_MINOR}.${SPI_PATCHLEVEL}
#export SPI_VERSION=BETA

#----- Library locations
export SPI_PATH
export SPI_SHARED=${SPI_PATH}/Lib/${BASE_ARCH}
export LD_LIBRARY_PATH=${SPI_SHARED}:${SPI_SHARED}/TclTk/lib:$LD_LIBRARY_PATH
export TCLLIBPATH="${SPI_SHARED} ${SPI_SHARED}/TclTk/lib ${SPI_SHARED}/TclTk/lib/tcl${TCL_VERSION} ${SPI_SHARED}/TclTk/lib/tk${TCL_VERSION} ${SPI_PATH}/Apps/Lib"
export TCL_LIBRARY="${SPI_SHARED}/TclTk/lib/tcl${TCL_VERSION}"

#----- GDB data
if [[ ${GDB_PATH} = "" ]] ; then
   export GDB_PATH=/cnfs/ops/production/cmoe/geo/DBGeo/data
#   export GDB_PATH=/data/cmds/afsm/gis_le
fi

#----- GDAL data
export GDAL_DATA=${SPI_PATH}/Lib/Data/gdal
export GDAL_PAM_ENABLED=NO
export CPL_LOG=/dev/null

#----- Force BURP table dir since it's hardcoded with RMN and problem outside cmc
export MA_TABLEBURP_PERSONNELLE=${SPI_PATH}/Lib/Data/bufr/table_b_bufr

#----- Force GRIB Tables
export GRIB_TEMPLATE_PATH=${SPI_PATH}/Lib/Data/grib/templates
export GRIB_DEFINITION_PATH=${SPI_PATH}/Lib/Data/grib/definitions

#----- EER Modeling variables
export EER_DIRBIN=${SPI_PATH}/Bin/${BASE_ARCH}
export EER_DIRSCRIPT=${SPI_PATH}/Script
