#----- Definitions
export ORDENV_PLAT=${ORDENV_PLAT:=`uname -s`_`uname -m`}

export TCL_VERSION=8.6

export SPI_MAJOR=7
export SPI_MINOR=6
export SPI_PATCHLEVEL=3
export SPI_STATE=""
export SPI_VERSION=${SPI_MAJOR}.${SPI_MINOR}.${SPI_PATCHLEVEL}

#----- Library locations
export SPI_PATH
export SPI_LIB=${SPI_PATH}/lib/${ORDENV_PLAT}
export LD_LIBRARY_PATH=${SPI_LIB}:${SPI_LIB}/TclTk/lib:$LD_LIBRARY_PATH
export TCLLIBPATH="${SPI_LIB} ${SPI_LIB}/TclTk/lib ${SPI_LIB}/TclTk/lib/tcl${TCL_VERSION} ${SPI_LIB}/TclTk/lib/tk${TCL_VERSION} ${SPI_PATH}/tcl/Lib ${TCLLIBPATH}"
export TCL_LIBRARY="${SPI_LIB}/TclTk/lib/tcl${TCL_VERSION}"

#----- GDB data
if [[ ${GDB_PATH} = "" ]] ; then
   export GDB_PATH=/cnfs/ops/production/cmoe/geo/DBGeo/data
#   export GDB_PATH=/data/cmds/afsm/gis_le
fi

#----- GDAL data
export GDAL_DATA=${SPI_PATH}/share/gdal
export GDAL_PAM_ENABLED=NO
export CPL_LOG=/dev/null

#----- Force BURP table dir since it's hardcoded with RMN and problem outside cmc
export MA_TABLEBURP_PERSONNELLE=${SPI_PATH}/share/bufr/table_b_bufr

#----- Force GRIB API Tables
export GRIB_TEMPLATE_PATH=${SPI_PATH}/share/grib/templates
export GRIB_DEFINITION_PATH=${SPI_PATH}/share/grib/definitions
