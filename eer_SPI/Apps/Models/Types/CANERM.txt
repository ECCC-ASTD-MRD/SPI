#============================================================================
# Environnement Canada
# Centre Meteorologique Canadien
# 2100 Trans-Canadienne
# Dorval, Quebec
#
# Projet     : Interface pour la gestion des experiences.
# Fichier    : <CANERM.txt>
# Creation   : Octobre 1999 - J.P. Gauthier - CMC/CMOE
#
# Description: Description des textes et labels relatives au module CANERM.
#
# Remarques  :
#
#============================================================================

namespace eval CANERM {
   variable Lbl
   variable Msg
   variable Error
   variable Title
   variable Bubble

   #----- Definitions des labels

   set Lbl(Start)              { "Temps de départ (UTC)" "Starting Time (UTC)" }
   set Lbl(Integration)        { "Intégration" "Integration" }
   set Lbl(SimDuration)        { "Durée de simulation    (h)" "Simulation duration (h)" }
   set Lbl(OutputTimeStepHour) { "Pas de temps de sortie (h)" "Output time step    (h)" }
   set Lbl(OtherParameters)    { "Autres paramètres" "Other Parameters" }
   set Lbl(MeteoDelta)         { " Dt (h)"          " Dt (h)" }

   set Lbl(At)          { "A" "At" }
   set Lbl(Add)         { "Ajouter" "Add" }
   set Lbl(Cancel)      { "Annuler" "Cancel" }
   set Lbl(Close)       { "Fermer" "Close" }
   set Lbl(Continue)    { "Continuer ..." "Continue ..." }
   set Lbl(Delta)       { "Delta (HR)" "Delta (HR)" }
   set Lbl(Deviation)   { "Écart-Type" "Std Deviation" }
   set Lbl(Duration)    { "Durée" "Duration" }
   set Lbl(Elevation)   { "Hauteur(M)" "Height(M) " }
   set Lbl(Event)       { "Évenement  " "Event type " }
   set Lbl(Time)        { "Temporelle" "Time" }
   set Lbl(Intensity)   { "Intensité" "Intensity" }
   set Lbl(Launch)      { "Lancer" "Launch" }
   set Lbl(Host)        { "Hôte " "Host " }
   set Lbl(Queue)       { "Queue" "Queue" }
   set Lbl(Maps)        { "Carte" "Map" }
   set Lbl(Mean)        { "Moyenne" "Average" }
   set Lbl(Mode)        { "Mode" "Mode" }
   set Lbl(Meteo)       { "Météo      " "Meteo      " }
   set Lbl(No)          { "Non" "No" }
   set Lbl(Emission)    { "Émission" "Emission" }
   set Lbl(Grid)        { "Grille     " "Grid       " }
   set Lbl(Suppress)    { "Supprimer" "Delete" }
   set Lbl(Vertical)    { "Vertical" "Vertical" }
   set Lbl(Result)      { "Résultats" "Results" }
   set Lbl(Relaunch)    { "Relancer ..." "Relaunch ..." }
   set Lbl(Yes)         { "Oui" "Yes" }
   set Lbl(Date)        { "Date de début" "Start date" }
   set Lbl(DateEnd)     { "Date de fin" "Ending date" }
   set Lbl(Pollutant)   { "Polluants" "Pollutants" }
   set Lbl(Product)     { "Produits" "Products" }
   set Lbl(Function)    { "Fonction de dispersion" " Dispersive function" }
   set Lbl(Params)      { "Paramêtres" "Parameters" }

   set Lbl(Status0)     { "Continuable"  "Continue" }
   set Lbl(Status1)     { "Terminée"     "Terminated" }
   set Lbl(Status2)     { "En Exécution" "Running" }
   set Lbl(Status3)     { "Suspendue"    "Suspended" }
   set Lbl(Status4)     { "Requête"      "Request" }

   set Lbl(Info)    {{"Localisation            :"\
                      "Date d'accident         :"\
                      "Date de simulation      :"\
                      "Durée de la simulation  :"\
                      "Mode                    :"\
                      "Echelle                 :"\
                      "Météo                   :"\
                      "Espèce(s)               :"\
                      "Intensite(s)            :"\
                      "Distribution temporelle :"\
                      "Distribution verticale  :"\
                      "Période de relachement  :"\
                      "Sortie des résultats    :"\
                      "Type d'évenement        :"\
                      "Status                  :"}\
                     {"Localisation           :"\
                      "Accident date          :"\
                      "Simulation date        :"\
                      "Duration of simulation :"\
                      "Mode                   :"\
                      "Scale                  :"\
                      "Meteo                  :"\
                      "Specie(s)              :"\
                      "Intensity(ies)         :"\
                      "Time distribution      :"\
                      "Vertical distribution  :"\
                      "Release duration       :"\
                      "Output of results      :"\
                      "Event type             :"\
                      "Status                 :"}}

   #----- Definitions des bulles d'aides

   set Bubble(Date)      { "Sélection de la date d'accident"
                           "Select the accident date" }
   set Bubble(Time)      { "Sélection de l'heure de l'accident"
                           "Select the accident houre" }
   set Bubble(Nb)        { "Nombre de polluants selectionné"
                           "Number of selected pollutant" }
   set Bubble(Add)       { "Affiche une liste de polluants pouvant être ajouter"
                           "Display a list of available pollutant" }
   set Bubble(Del)       { "Supprime le polluant actuellement sélectionné"
                           "Remove the current pollutant form the list" }
   set Bubble(Pol)       { "Liste des polluants sélectionnées"
                           "Selected pollutant list" }
   set Bubble(Intensity) { "Intensité du polluant actuellement sélectionné"
                           "Current pollutant intensity" }
   set Bubble(Height)    { "Hauteur de la source en mêtres"
                           "Source height in meters" }
   set Bubble(Vertical)  { "Sélection de la fonction de distribution verticale"
                           "Select the vertical distribution fonction" }
   set Bubble(Delai)     { "Durée de la distribution temporelle en heures"
                           "Duration of the distribution in hours" }
   set Bubble(Temporal)  { "Sélection de la courbe de distribution temporelle"
                           "Select the time dependant distribution fonction" }
   set Bubble(Event)     { "Sélection du type d'évènement (A titre informatif)"
                           "Select the event type (As information label)" }
   set Bubble(Scale)     { "Sélection de la dimension de la grille de calcul"
                           "Select the calculation grid size" }
   set Bubble(Path)      { "Sélection des répertoires de données météorologiques"
                           "Select the meteorological data path" }
   set Bubble(Launch)    { "Lancer la simulation CANERM"
                           "Launch the CANERM simulation" }
   set Bubble(List)      { "Liste des fichier disponibles pour la simulation"
                           "Available data files for this simulation" }
   set Bubble(SimIntegration)     { "Intégration du modèle." "Model integration." }
   set Bubble(SimDuration)        { "Durée de la simulation (hr)." "Simulation duration (hr)." }
   set Bubble(OutputTimeStepHour) { "Pas de temps de sortie (heure) des données." "Data output time step (hour)." }
   set Bubble(Meteo)              { "Type de modèle météorologique." "Type of meteorological model." }
   set Bubble(MeteoDelta)         { "Interval de temps entre les fichiers\ndes données météorologiques (hr)." "Time interval between meteorological\ndata files (hr)." }

   #----- Definitions des messages

   set Msg(Meteo)        { "Création des données météo en cours" "Creating meteorological data" }
   set Msg(JobSusp)      { "Le script de lancement à été crée. Voulez-vous générer les données météos sur le serveur ?" \
                           "The launch script has been created. Do you want to generate the meteo data on the server ?" }
   set Msg(LaunchDone)   { "Lancement effectué " "Launch done" }
   set Msg(JobInfo)      { "Récuperation du script" "Getting job script" }
   set Msg(JobCancel)    { "Lancement annulé" "Launch canceled" }
   set Msg(Launch)       { "Lancement de " "Launching " }
   set Msg(MetGet)       { "Vérification de la disponibilité des données météo en cours..." "Checking for meteorological data availability..." }

   #----- Definitions des erreurs

   set Error(EndDate)    { "Auncune date de fin n'a été sélectionnée" "No simulation end date has been selected" }
   set Error(Intensity)  { "L'intensité spécifiée n'est pas valide" "Invalid intensity" }
   set Error(NbIso)      { "Il n'y a aucun polluant sélectionné." "No pollutant selected." }
   set Error(Height)     { "L'élévation n'est pas valide et dépasse la limite de 30000 mêtres."
                           "Invalid elevation. It is over the 30000 meters limit." }
   set Error(Previous)   { "Les résultats d'une simulation précédente sont manquants.\
                            Les calculs de concentration, dépot et dose seront donc invalide."
                           "Previous simulation results missing. The concentrations, depot and dosage\
                            calculus will be wrong." }
}
