#============================================================================
# Environnement Canada - Service meteorologique du Canada
# Centre meteorologique canadien
# 2121 Route Trans-canadienne
# Dorval, Quebec
# H9P 1J3
#
# Projet     : Interface pour la gestion des experiences.
# Fichier    : <MLDPn.txt>
# Creation   : Octobre 1999 - J.P. Gauthier - CMC/CMOE
#
# Description: Description des textes et labels relatives au module MLDP.
#
# Remarques  :
#
#============================================================================

namespace eval MLDPn {
   variable Lbl
   variable Msg
   variable Error
   variable Warning
   variable Bubble

   #----- Definitions des labels

   set Lbl(Yes)        { "Oui" "Yes" }
   set Lbl(No)         { "Non" "No " }
   set Lbl(DiskPath)   { "Répertoire              " "directory           " }
   set Lbl(DiskNeed)   { "Espace disque critique  " "Critical disk space " }
   set Lbl(DiskAvail)  { "Espace disque disponible" "Available disk space" }

   set Lbl(Product)             { "Produits"  "Products" }
   set Lbl(Result)              { "Résultats" "Results" }
   set Lbl(Continue)            { "Continuer ..." "Continue ..." }
   set Lbl(Relaunch)            { "Relancer ..." "Relaunch ..." }
   set Lbl(Suppress)            { "Supprimer" "Delete" }

   set Lbl(Model)               { "Modèle" "Model" }
   set Lbl(Source)              { "Source" "Source" }
   set Lbl(Start)               { "Temps de départ (UTC)" "Starting Time (UTC)" }
   set Lbl(Date)                { "Date" "Date" }
   set Lbl(Delta)               { "Delta (HR)" "Delta (HR)" }
   set Lbl(Hour)                { "Heure " "Hour " }
   set Lbl(At)                  { "A " "At " }
   set Lbl(Integration)         { "Intégration" "Integration" }
   set Lbl(DiffKernel)          { "Noyau de diffusion          " "Diffusion kernel         " }
   set Lbl(SimDuration)         { "Durée de simulation    (h)  " "Simulation duration (h)  " }
   set Lbl(OutputTimeStepMin)   { "Pas de temps de sortie (min)" "Output time step    (min)" }
   set Lbl(ModelTimeStepMin)    { "Pas de temps du modèle (min)" "Model time step     (min)" }
   set Lbl(OtherParameters)     { "Autres paramètres" "Other Parameters" }
   set Lbl(Event)               { "Événement   "     "Event type " }
   set Lbl(Virus)               { "Virus       "     "Virus type " }
   set Lbl(Scale)               { "Grille      "     "Grid       " }
   set Lbl(Meteo)               { "Météorologie"     "Meteorology" }
   set Lbl(MeteoDelta)          { " Dt (h)"          " Dt (h)" }
   set Lbl(OutCV)               { "Niveaux  (m)"     "Levels  (m)" }
   set Lbl(VarMesoscale)        { "Variance  (m²/s²)" "Variance (m²/s²)" }
   set Lbl(Timescale)           { "Échelle temps (s)" "Time scale   (s)" }
   set Lbl(ReflectionLevel)     { "Réflexion   (hyb)" "Reflection (hyb)" }

   set Lbl(Emission)            { "Émission" "Emission" }
   set Lbl(Column)              { "Colonne d'émission" "Emission column" }
   set Lbl(EmNumberParticles)   { "Nombre de particules     " "Number of particles      " }
   set Lbl(EmDensity)           { "Densité           (µg/m³)" "Density           (µg/m³)" }
   set Lbl(EmHeight)            { "Hauteur maximale      (m)" "Maximum height        (m)" }
   set Lbl(EmMass)              { "Masse émise          (µg)" "Mass released        (µg)" }
   set Lbl(EmRadius)            { "Rayon de la colonne   (m)" "Column radius         (m)" }
   set Lbl(EmSizeDist)          { "Distribution de la taille" "Size distribution        " }
   set Lbl(EmVerticalDist)      { "Distribution verticale   " "Vertical distribution    " }
   set Lbl(EmInter)             { "Durée de l'intervalle    " "Interval duration        " }
   set Lbl(NbIsotopes)          { "Nombre d'isotopes        " "Number of isotopes       " }

   set Lbl(EmInterUnit0)        { "(h)" "(h)" }
   set Lbl(EmInterUnit1)        { "(s)" "(s)" }

   set Lbl(EmIsAutoNP)          { "Distribution automatique des particules" "Automatic particles distribution" }
   set Lbl(EmIsEm)              { "Type d'intervalle        " "Type of interval         " }

   set Lbl(GlobalParameters)    { "Paramètres globaux" "Global parameters" }
   set Lbl(IntervalParameters)  { "Paramètres de l'intervalle" "Interval parameters" }

   set Lbl(Scenario)            { "Scénario d'émission"  "Emission scenario" }
   set Lbl(ScenarioMgnt)        { "Gestion des scénarios"  "Scenario management" }
   set Lbl(ScenarioName)        { "Nom du scénario     " "Scenario name         " }
   set Lbl(NbIntervals)         { "Nombre d'intervalles" "Number of intervals   " }
   set Lbl(TotalDuration)       { "Durée totale     (s)" "Total duration     (s)" }
   set Lbl(EffectiveDuration)   { "Durée effective  (s)" "Effective duration (s)" }
   set Lbl(Edit)                { "Éditer" "Edit" }
   set Lbl(EditInter)           { "Éditer intervalle..." "Edit interval..." }

   set Lbl(Add)                 { "Ajouter" "Add" }
   set Lbl(Duration)            { "Durée (s)" "Duration (s)" }
   set Lbl(Period)              { "Type de période" "Type of period" }
   set Lbl(ReleaseRateAccident) { "Taux de relâchement des isotopes\n(unité/h)" "Isotopes release rates\n(unit/h)" }
   set Lbl(ReleaseRateVirus)    { "Taux de relâchement\ndu virus (unité/h)" "Virus release rates\n(unit/h)" }
   set Lbl(ReleaseRate)         { "Taux de relâchement      " "Release Rate             " }
   set Lbl(ReleaseRateUnit0)    { "(unité/h)" "(unit/h)" }
   set Lbl(ReleaseRateUnit1)    { "(unité)" "(unit)" }
   set Lbl(TotalQuantity)       { "Quantité totale\n(unité)" "Total quantity\n(unit)" }
   set Lbl(Apply)               { "Appliquer" "Apply" }
   set Lbl(Cancel)              { "Annuler" "Cancel" }
   set Lbl(Close)               { "Fermer" "Close" }
   set Lbl(Yes)                 { "Oui" "Yes" }
   set Lbl(No)                  { "Non" "No " }
   set Lbl(Warning)             { "Avertissement" "Warning" }
   set Lbl(Interval)            { "Intervalle" "Interval" }

   set Lbl(Overwrite)           { "Remplacer" "Overwrite" }
   set Lbl(Save)                { "Sauvegarder" "Save" }
   set Lbl(SaveAs)              { "Sauvegarder sous..." "Save as..." }
   set Lbl(Ignore)              { "Ignorer" "Ignore" }
   set Lbl(Clear)               { "Effacer" "Clear" }

   set Lbl(AxisX)               { "Durée (h) depuis" "Duration (h) since" }
   set Lbl(AxisY)               { "Hauteur max (m)" "Max height (m)" }

   #----- Definitions des bulles d'aides

   set Bubble(Source)                 { "Nom de la source." "Name of the source." }
   set Bubble(SelectSource)           { "Sélectionner la source." "Select the source." }
   set Bubble(StartTime)              { "Temps de départ (UTC) de la simulation." "Simulation starting time (UTC)." }
   set Bubble(StartDate)              { "Date du début de la simulation." "Date of beginning of simulation." }
   set Bubble(StartHour)              { "Heure et minutes de la date de simulation." "Hour and minutes of simulation date." }
   set Bubble(SimIntegration)         { "Intégration du modèle." "Model integration." }
   set Bubble(SimDuration)            { "Durée de la simulation (hr)." "Simulation duration (hr)." }
   set Bubble(OutputTimeStepMin)      { "Pas de temps de sortie (min) des données." "Data output time step (min)." }
   set Bubble(ModelTimeStepMin)       { "Pas de temps interne du modèle (min)." "Internal model time step (min)." }

   set Bubble(OtherParameters)        { "Autres paramètres." "Other parameters." }
   set Bubble(Event)                  { "Sélection du type d'événement (À titre informatif)." "Select the event type (As information label)." }
   set Bubble(Virus)                  { "Sélection du type de virus." "Select the virus type." }
   set Bubble(Scale)                  { "Résolution (km) de la grille\npolaire stéréographique." "Resolution (km) of the polar stereographic grid." }
   set Bubble(Meteo)                  { "Type de modèle météorologique." "Type of meteorological model." }
   set Bubble(MeteoDelta)             { "Interval de temps entre les fichiers\ndes données météorologiques (hr)." "Time interval between meteorological\ndata files (hr)." }
   set Bubble(MoveGrid1)              { "Permettre de centrer automatiquement la grille\nsur la source sélectionnée sur la sphère terrestre." "Allow to center the the grid over the\nselected source on the earth's sphere." }
   set Bubble(MoveGrid2)              { "Permettre de modifier l'emplacement de\nla grille sur la sphère terrestre." "Allow to modify the location of\nthe grid on the earth's sphere." }
   set Bubble(ModifyPath)             { "Permettre de modifier les répertoires\ndes modèles météorologiques." "Allow to modify the meteorological\nmodels directories." }
   set Bubble(OutCV)                  { "Liste des niveaux verticaux servant à définir les couches\ndans l'atmosphère utilisées pour calculer les\nconcentrations volumiques.\n\nNote: Les concentrations sont calculées à chaque point\nde grille pour chaque couche spécifiée de l'atmosphère." "List of vertical levels which define the layers\nin the atmosphere used to calculate the volumic\nconcentrations.\n\nNote: The concentrations are calculated at each grid\npoint for each specified layer of the atmosphere." }
   set Bubble(VarMesoscale)           { "Variance (m²/s²) des vitesses horizontales des vents\nservant à modéliser les fluctuations à méso-échelle." "Horizontal wind velocity variance (m²/s²) used\nin the modelling of mesoscale fluctuations." }
   set Bubble(Timescale)              { "Échelle de temps lagrangienne (s) servant\nà modéliser les fluctuations à méso-échelle." "Lagrangian time scale (s) used in the\nmodelling of mesoscale fluctuations." }
   set Bubble(ReflectionLevel)        { "Niveau (hyb|eta|sig) inférieur de réflexion\ndes particules dans l'atmopshère." "Bottom reflection level (hyb|eta|sig) of\nparticles in the atmosphere." }

   set Bubble(EmissionGlobal)         { "Paramètres globaux d'émission." "Global emission parameters" }
   set Bubble(EmSizeDist)             { "Sélectionner le type de distribution de la taille des particules\npour modéliser l'effet de la vitesse de sédimentation et pour MLDP0, l'effet du dépôt gravitationnel selon la loi de Stokes et ." "Select the type of particle size distribution\nto model the effect of particle settling velocity and for MLDP0, the effect of gravitational settling according to Stokes' law." }
   set Bubble(EmDensity)              { "Densité d'une particule (µg/m³)." "Particle density (µg/m³)." }
   set Bubble(EmVerticalDist)         { "Sélectionner le type de distribution verticale\npour modéliser le panache d'émission." "Select the type of vertical distribution\nto model the released plume." }
   set Bubble(EmIsAutoNP)             { "Détermine la répartition des particules (automatique ou manuelle).\nLa répartition automatique distribue les particules en fonction de la masse de chaque interval d'émission." "Switch between automatic or manual particles distribution.\nAutomatic distribution is done proportionnaly to the released mass of emission intervals." }
   set Bubble(EmGlobalNP)             { "Nombre de particules à être automatiquement réparties parmis les intervals d'émission." "Number of particles to be distributed amongst emission intervals" }

   set Bubble(EmissionDetails)        { "Paramètre de l'interval d'émission courant." "Current emission interval parameters." }
   set Bubble(DurationInter)          { "Durée de l'intervalle." "Interval duration." }
   set Bubble(EmIsEm)                 { "Type de période d'émission (relâchement ou accalmie)." "Type of release period (release or lull)." }
   set Bubble(EmNumberParticles)      { "Nombre de particules relâchées." "Number of released particles." }
   set Bubble(EmHeight)               { "Hauteur maximale du panache (m)." "Maximum plume height (m)." }
   set Bubble(EmRadius)               { "Rayon de la colonne d'émission (m)." "Emission column radius (m)." }
   set Bubble(MassMode)               { "Sélectionner le mode pour le calcul de la masse totale relâchée (µg).\n\n - Formule empirique de Sparks et al. (1997): La masse totale relâchée ne peut pas être éditée;\n   elle dépend de la densité d'une particule, de la hauteur du panache et de la durée effective d'émission.\n - Édition: La masse totale relâchée peut être éditée." "Select the mode for total released mass calculation (µg).\n\n - Empirical formula of Sparks et al. (1997): Total mass released cannot be edited;\n   it is a function of particle density, plume height and effective emission duration.\n - Edit: Total mass released can be edited." }
   set Bubble(ReleaseRateVirus)       { "Taux de relâchement du virus." "Virus release rate." }

   set Bubble(Isotopes)               { "Détails concernant le relâchement des isotopes pour cet interval." "Details concerning the release of isotopes for this time interval." }
   set Bubble(ReleaseRateIso)         { "Taux de relâchement de l'isotope." "Isotope release rate." }
   set Bubble(Isotope)                { "Symbol de l'isotope radioactif." "Radioactive isotope symbol." }
   set Bubble(IsoAdd)                 { "Ajouter un nouvel isotope radioactif." "Add a new radioactive isotope." }
   set Bubble(IsoDelete)              { "Supprimer l'isotope radioactif sélectionné." "Delete the selected radioactive isotope." }

   set Bubble(Cancel)                 { "Annuler les modifications apportées." "Cancel the modifications made." }
   set Bubble(Apply)                  { "Appliquer les changements." "Apply changes." }

   set Bubble(Scenario)               { "Gestion des scénario d'émission." "Emission scenario management." }
   set Bubble(ScenarioName)           { "Nom du scénario d'émission." "Name of the emission scenario." }
   set Bubble(ScenarioSelect)         { "Sélectionner le nom du scénario d'émission." "Select the name of the emission scenario." }
   set Bubble(ScenarioClear)          { "Vider le scénario d'émission courant." "Clear the current release scenario." }
   set Bubble(ScenarioDelete)         { "Supprimer le scénario d'émission sélectionné." "Delete the selected release scenario." }
   set Bubble(ScenarioSave)           { "Sauvegarder le scénario d'émission courant." "Save the current release scenario." }

   set Bubble(EmissionScenario)       { "Détails du scénario d'émission." "Emission scenario details." }
   set Bubble(EmissionIntervalAdd)    { "Ajouter un interval d'émission." "Add an emission interval." }
   set Bubble(GraphZoom)              { "Augmenter ou réduire le facteur de zoom de l'axe des X." "Change X axis zoom factor." }
   set Bubble(PickInterval)           { "Sélectionne un interval d'émission à éditer." "Select an emission interval to edit." }
   set Bubble(DiffKernel)             { "Sélectionne un noyau de diffusion a utiliser." "Select a diffusion kernel to use." }

   #----- Definitions des messages

   set Msg(MetGet)       { "Vérification de la disponibilité des données météorologiques en cours..." "Checking for meteorological data availability..." }
   set Msg(MetProcess)   { "Interpolation des données météo en cours ..." "Processing meteorological data ..." }
   set Msg(Meteo)        { "Création des données météo en cours ..." "Creating meteorological data ..." }
   set Msg(LaunchDone)   { "Lancement effectué " "Launch done" }
   set Msg(ScenarioName) { "Sélectionner le nom du scénario d'émission." "Select the name of the emission scenario." }

   #----- Definitions des erreurs

   set Error(UnitSeconds)                       "s."
   set Error(UnitMinutes)                       "min."
   set Error(UnitHours)                         "h."
   set Error(UnitMeters)                        "m."
   set Error(UnitM2PS2)                         "m²/s²."
   set Error(UnitHybEtaSig)                     "hyb|eta|sig."
   set Error(UnitDensity)                       { "µg/m³." "µg/m³." }
   set Error(UnitMass)                          { "µg." "µg." }
   set Error(UnitRate)                          { "unité/h." "unit/h." }


   set Error(EndDate)                           { "Auncune date de fin n'a été sélectionnée" "No simulation end date has been selected" }
   set Error(SimDuration)                       { "La durée de simulation (spécifiée en heures) doit être un nombre entier positif.\n\n\tDurée de simulation :" \
                                                  "The simulation duration  (specified in hours) must be a positive integer number.\n\n\tSimulation duration :" }
   set Error(SimDurationOutRange)               { "La valeur de la durée de simulation (spécifiée en heures) déborde la capacité mémoire d'un nombre entier.\n\n\tDurée de simulation :" \
                                                  "The simulation duration value (specified in hours) under/overflows the memory capacity of an integer number.\n\n\tSimulation duration :" }
   set Error(SimDurationDelta)                  { "La durée de simulation doit être inférieure ou égale à l'interval de temps entre les données des fichiers météorologiques.\n\n" \
                                                  "The simulation duration must be lower or equal to the time interval between meteorological data files.\n\n" }
   set Error(SimDurationDelta2)                 { "\tDurée de simulation     :" "\tSimulation duration :" }
   set Error(SimDurationDelta3)                 { "\tInterval de temps météo :" "\tMeteo time interval :" }
   set Error(SimDurationOutputTimeStep)         { "La durée de simulation doit être supérieure ou égale au pas de temps de sortie des données.\n\n" \
                                                  "The simulation duration must be greater or equal to the output time step.\n\n" }
   set Error(SimDurationOutputTimeStep2)        { "\tDurée de simulation    :" "\tSimulation duration :" }
   set Error(SimDurationOutputTimeStep3)        { "\tPas de temps de sortie :" "\tOutput time step    :" }
   set Error(ModelTimeStepMin)                  { "Le pas de temps interne du modèle (spécifié en minutes) doit être un nombre entier positif.\n\n\tPas de temps du modèle :" \
                                                  "The internal model time step (specified in minutes) must be a positive integer number.\n\n\tModel time step :" }
   set Error(ModelTimeStepMinOutRange)          { "La valeur du pas de temps interne du modèle (spécifié en minutes) déborde la capacité mémoire d'un nombre entier.\n\n\tPas de temps du modèle :" \
                                                  "The internal model time step value (specified in minutes) under/overflows the memory capacity of an integer number.\n\n\tModel time step :" }
   set Error(OutputTimeStepMin)                 { "Le pas de temps de sortie (spécifié en minutes) doit être un nombre entier positif.\n\n\tPas de temps de sortie :" \
                                                  "The output time step (specified in minutes) must be a positive integer number.\n\n\tOutput time step :" }
   set Error(OutputTimeStepMinOutRange)         { "La valeur du pas de temps de sortie (spécifié en minutes) déborde la capacité mémoire d'un nombre entier.\n\n\tPas de temps de sortie :" \
                                                  "The output time step value (specified in minutes) under/overflows the memory capacity of an integer number.\n\n\tOutput time step :" }
   set Error(ModelTimeStep1hour)                { "Le pas de temps interne du modèle (spécifié en minutes) doit être inférieur ou égal à une heure (60 minutes).\n\n\tPas de temps du modèle :" \
                                                  "The internal model time step (specified in minutes) must be lower or equal to an hour (60 minutes).\n\n\tModel time step :" }
   set Error(ModelTimeStep1hourDiv)             { "Le pas de temps interne du modèle (spécifié en minutes) doit être un diviseur entier d'une heure (60 minutes).\n\n\tPas de temps du modèle :" \
                                                  "The internal model time step (specified in minutes) must be an integer divisor of an hour (60 minutes).\n\n\tModel time step :" }
   set Error(ModelTimeStepOutputTimeStep)       { "Le pas de temps interne du modèle doit être inférieur ou égal au pas de temps de sortie.\n\n" \
                                                  "The internal model time step must be lower or equal to the output time step.\n\n" }
   set Error(ModelTimeStepOutputTimeStep2)      { "\tPas de temps du modèle :" "\tModel time step  :" }
   set Error(ModelTimeStepOutputTimeStep3)      { "\tPas de temps de sortie :" "\tOutput time step :" }
   set Error(OutputTimeStepModelTimeStep)       { "Le pas de temps de sortie doit être un multiple entier du pas de temps interne du modèle.\n\n" \
                                                  "The output time step must be an integer multiple of the internal model time step.\n\n" }
   set Error(OutputTimeStepModelTimeStep2)      { "\tPas de temps de sortie :" "\tOutput time step :" }
   set Error(OutputTimeStepModelTimeStep3)      { "\tPas de temps du modèle :" "\tModel time step  :" }

   set Error(OutCV1)                   { "Le nombre de niveaux verticaux pour le calcul des concentrations à l'intérieur des couches doit être supérieur à 1\n\n" "The number of vertical levels for concentration calculations inside layers must be greater than 1.\n\n" }
   set Error(OutCV2)                   { "Les niveaux verticaux (spécifiés en mètres) doivent être des nombres réels supérieurs ou égal à 0.\n\n\tNiveau vertical :" "The vertical levels (specified in meters) must be float numbers greater or equal to 0.\n\n\tVertical level :" }
   set Error(OutCV3)                   { "Les niveaux verticaux (spécifiés en mètres) doivent être ordonnés en ordre croissants.\n\n\tNiveaux verticaux :" "The vertical levels (specified in meters) must be sorted in increasing order.\n\n\tVertical levels :" }
   set Error(OutCVRange)               { "Une valeur dans la liste des niveaux verticaux déborde la capacité mémoire d'un nombre réel.\n\n\tNiveau vertical :" "A value in the list of vertical levels under/overflows the memory capacity of a float number.\n\n\tVertical Level :" }
   set Error(VarMesoscale)                      { "La variance (spécifiée en m²/s²) doit être un nombre réel supérieur ou égal à 0.\n\n\tVariance :" \
                                                  "The variance (specified in m²/s²) must be a float number greater or equal to 0.\n\n\tVariance :" }
   set Error(VarMesoscale2)                     { "La variance (spécifiée en m²/s²) doit être inférieure ou égale à 10.0 m²/s².\n\n\tVariance :" \
                                                  "The variance (specified in m²/s²) must be lower or equal to 10.0 m²/s².\n\n\tVariance :" }
   set Error(VarMesoscaleRange)                 { "La valeur de la variance (spécifiée en m²/s²) déborde la capacité mémoire d'un nombre réel.\n\n\tVariance :" \
                                                  "The value of the variance (specified in m²/s²) under/overflows the memory capacity of a float number.\n\n\tVariance :" }
   set Error(Timescale)                         { "L'échelle de temps lagrangienne (spécifiée en s) doit être un nombre réel supérieur à 0.\n\n\tÉchelle de temps :" \
                                                  "The Lagrangian time scale (specified in s) must be a float number greater than 0.\n\n\tTime scale :" }
   set Error(Timescale2)                        { "L'échelle de temps lagrangienne (spécifiée en s) doit être inférieure ou égale à 21600 s.\n\n\tÉchelle de temps :" \
                                                  "The Lagrangian time scale (specified in s) must be lower or equal to 21600 s.\n\n\tTime scale :" }
   set Error(TimescaleRange)                    { "La valeur de l'échelle de temps lagrangienne (spécifiée en s) déborde la capacité mémoire d'un nombre réel.\n\n\tÉchelle de temps :" \
                                                  "The value of the Lagrangian time scale (specified in s) under/overflows the memory capacity of a float number.\n\n\tTime scale :" }
   set Error(ReflectionLevel)                   { "Le niveau inférieur de réflexion des particules dans l'atmosphère (spécifiée en hyb|eta|sig) doit être un nombre réel positif.\n\n\tNiveau de réflexion :" \
                                                  "The bottom reflection level of particles in the atmosphere (specified in hyb|eta|sig) must be a positive float number.\n\n\tReflection level :" }
   set Error(ReflectionLevel2)                  { "Le niveau inférieur de réflexion des particules dans l'atmosphère (spécifiée en hyb|eta|sig) doit être compris dans l'intervalle \[0.9900, 1.0000\].\n\n\tNiveau de réflexion :" \
                                                  "The bottom reflection level of particles in the atmosphere (specified in hyb|eta|sig) must fall within the range \[0.9900, 1.0000\].\n\n\tReflection level :" }
   set Error(ReflectionLevelRange)              { "La valeur du niveau inférieur de réflexion des particules dans l'atmosphère (spécifiée en hyb|eta|sig) déborde la capacité mémoire d'un nombre réel.\n\n\tNiveau de réflexion :" \
                                                  "The value of the bottom reflection level of particles in the atmosphere (specified in hyb|eta|sig) under/overflows the memory capacity of a float number.\n\n\tReflection level :" }

   set Error(EmNumberParticles)                 { "Le nombre de particules doit être un nombre entier positif et supérieur ou égal à 1000.\n\n\tNombre de particules :" \
                                                  "The number of particles must be a positive integer number greater or equal to 1000.\n\n\tNumber of particles :" }
   set Error(EmNumberParticles2)                { "Le nombre de particules doit être un nombre entier multiple de 1000.\n\n\tNombre de particules :" \
                                                  "The number of particles must be an integer multiple number of 1000.\n\n\tNumber of particles :" }
   set Error(EmNumberParticles3)                { "Le nombre de particules doit être inférieur (ou égal) à la valeur limite maximale déclarée dans le programme.\n\n\tNombre de particules         :" \
                                                  "The number of particles must be less than (or equal to) maximum limit value declared in the program.\n\n\tNumber of particles         :" }
   set Error(EmNumberParticles4)                { "\tNombre maximum de particules :" \
                                                  "\tMaximum number of particles :" }
   set Error(EmNumberParticlesOutRange)         { "La valeur du nombre de particules déborde la capacité mémoire d'un nombre entier.\n\n\tNombre de particules :" \
                                                  "The value of the number of particules under/overflows the memory capacity of an integer number.\n\n\tNumber of particules :" }
   set Error(EmDensity)                         { "La densité d'une particule (spécifiée en µg/m³) doit être un nombre réel positif.\n\n\tDensité :" \
                                                  "The particle density (specified in µg/m³) must be a positive float number.\n\n\tDensity :" }
   set Error(EmDensityOutRange)                 { "La valeur de la densité (spécifiée en µg/m³) déborde la capacité mémoire d'un nombre réel.\n\n\tDensité :" \
                                                  "The particle density value (specified in µg/m³) under/overflows the memory capacity of a float number.\n\n\tDensity :" }
   set Error(Height)                            { "La hauteur maximale du panache (spécifiée en mètres) doit être un nombre réel positif.\n\n\tHauteur maximale :" \
                                                  "The maximum height of the plume (specified in meters) must be a positive float number.\n\n\tMaximum height :" }
   set Error(Height2)                           { "La hauteur maximale du panache (spécifiée en mètres) doit être inférieure ou égale à 30000 mètres.\n\n\tHauteur maximale :" \
                                                  "The maximum height of the plume (specified in meters) must be lower or equal to 30000 meters.\n\n\tMaximum height :" }
   set Error(HeightRange)                       { "La valeur de la hauteur maximale du panache (spécifiée en mètres) déborde la capacité mémoire d'un nombre réel.\n\n\tHauteur maximale :" \
                                                  "The value of the maximum height of the plume (specified in meters) under/overflows the memory capacity of a float number.\n\n\tMaximum height :" }
   set Error(Mass)                              { "La masse totale relâchée (spécifiée en µg) doit être un nombre réel positif.\n\n\tMasse totale relâchée :" \
                                                  "The total mass released (specified in µg) must be a positive float number.\n\n\tTotal mass released :" }
   set Error(MassRange)                         { "La valeur de la masse totale relâchée (spécifiée en µg) déborde la capacité mémoire d'un nombre réel.\n\n\tMasse totale relâchée :" \
                                                  "The total mass released value (specified in µg) under/overflows the memory capacity of a float number.\n\n\tTotal mass released :" }
   set Error(Radius)                            { "Le rayon de la colonne d'émission (spécifiée en mètres) doit être un nombre réel supérieur ou égal à 0.\n\n\tRayon de la colonne :" \
                                                  "The column radius (specified in meters) must be a float number greater or equal to 0.\n\n\tColumn radius :" }
   set Error(RadiusRange)                       { "La valeur du rayon de la colonne d'émission (spécifiée en mètres) déborde la capacité mémoire d'un nombre réel.\n\n\tRayon de la colonne :" \
                                                  "The column radius value (specified in meters) under/overflows the memory capacity of a float number.\n\n\tColumn radius :" }
   set Error(EffectiveDuration)                 { "La durée effective d'émission (spécifiée en secondes) doit être un nombre entier positif.\n\n\tDurée effective d'émission :" \
                                                  "The effective emission duration (specified in seconds) must be a positive integer number.\n\n\tEffective emission duration :" }
   set Error(EffectiveDurationRange)            { "La valeur de la durée effective d'émission (spécifiée en secondes) déborde la capacité mémoire d'un nombre entier.\n\n\tDurée effective d'émission :" \
                                                  "The effective emission duration value (specified in seconds) under/overflows the memory capacity of an integer number.\n\n\tEffective emission duration :" }
   set Error(TotalDuration)                     { "La durée total d'émission (spécifiée en secondes) doit être un nombre entier positif.\n\n\tDurée totale d'émission :" \
                                                  "The total emission duration (specified in seconds) must be a positive integer number.\n\n\tTotal emission duration :" }
   set Error(TotalDurationRange)                { "La valeur de la durée totale d'émission (spécifiée en secondes) déborde la capacité mémoire d'un nombre entier.\n\n\tDurée totale d'émission :" \
                                                  "The total emission duration value (specified in seconds) under/overflows the memory capacity of an integer number.\n\n\tTotal emission duration :" }
   set Error(EffectiveDuration2)                { "La durée effective d'émission (spécifiée en secondes) doit être inférieure ou égale à la durée totale d'émission (spécifiée en secondes, incluant les périodes d'accalmie).\n\n" \
                                                  "The effective emission duration (specified in seconds) must be lower or equal to the total emission duration (specified in seconds, including lull periods).\n\n" }
   set Error(EffectiveDuration3)                { "\tDurée effective d'émission :" "\tEffective emission duration :" }
   set Error(EffectiveDuration4)                { "\tDurée totale d'émission    :" "\tTotal emission duration     :" }
   set Error(ModelTimeStep1)                    { "Le pas de temps du modèle (converti en secondes) doit être inférieur ou égal à la durée totale d'émission (spécifiée en secondes).\n\n" \
                                                  "The model time step (converted in seconds) must be lower or equal to the total emission duration (specified in seconds).\n\n" }
   set Error(ModelTimeStep2)                    { "\tPas de temps du modèle  :" "\tModel time step         :" }
   set Error(ModelTimeStep3)                    { "\tDurée totale d'émission :" "\tTotal emission duration :" }
   set Error(ModelTimeStep4)                    { "Le pas de temps du modèle (converti en secondes) doit être inférieur ou égal à la durée effective d'émission (spécifiée en secondes).\n\n" \
                                                  "The model time step (converted in seconds) must be lower or equal to the effective emission duration (specified in seconds).\n\n" }
   set Error(ModelTimeStep5)                    { "\tPas de temps du modèle     :" "\tModel time step             :" }
   set Error(ModelTimeStep6)                    { "\tDurée effective d'émission :" "\tEffective emission duration :" }
   set Error(ModelTimeStep7)                    { "La durée totale d'émission (spécifiée en secondes) doit être un multiple entier du pas de temps du modèle (converti en secondes).\n\n" \
                                                  "The total emission duration (specified in seconds) must be an integer multiple of the model time step (converted in seconds).\n\n" }
   set Error(ModelTimeStep8)                    { "\tDurée totale d'émission :" "\tTotal emission duration :" }
   set Error(ModelTimeStep9)                    { "\tPas de temps du modèle  :" "\tModel time step         :" }
   set Error(ModelTimeStep10)                   { "La durée effective d'émission (spécifiée en secondes) doit être un multiple entier du pas de temps du modèle (converti en secondes).\n\n" \
                                                  "The effective emission duration (specified in seconds) must be an integer multiple of the model time step (converted in seconds).\n\n" }
   set Error(ModelTimeStep11)                   { "\tDurée effective d'émission :" "\tEffective emission duration :" }
   set Error(ModelTimeStep12)                   { "\tPas de temps du modèle     :" "\tModel time step             :" }
   set Error(Duration)                          { "Le scénario d'émission est invalide. La durée (spécifiée en heures) doit être un nombre entier positif. Veuillez modifier cette durée.\n\n\tDurée :" \
                                                  "The release scenario is invalid. The duration (specified in hours) must be a positive integer number. Please modify this duration.\n\n\tDuration :" }
   set Error(Duration2)                         { "Le scénario d'émission est invalide. La durée (spécifiée en heures) doit être supérieure ou égale au pas de temps interne du modèle (spécifié en minutes). Veuillez modifier la durée de cette période d'émission ou la valeur du pas de temps interne du modèle.\n\n" \
                                                  "The release scenario is invalid. The duration (specified in hours) must be greater or equal to the internal model time step (specified in minutes). Please modify this release period duration or the value of the internal model time step.\n\n" }
   set Error(Duration3)                         { "Le scénario d'émission est invalide. La durée (spécifiée en heures) doit être un multiple entier du pas de temps interne du modèle (spécifié en minutes). Veuillez modifier la durée de cette période d'émission ou la valeur du pas de temps interne du modèle.\n\n" \
                                                  "The release scenario is invalid. The duration (specified in hours) must be an integer multiple of the internal model time step (specified in minutes). Please modify this release period duration or the value of the internal model time step.\n\n" }
   set Error(Duration4)                         { "\tDurée de la période d'émission :" "\tRelease period duration :" }
   set Error(Duration5)                         { "\tPas de temps du modèle         :" "\tModel time step         :" }
   set Error(DurationRange)                     { "Le scénario d'émission est invalide. La valeur de la durée (spécifiée en secondes) déborde la capacité mémoire d'un nombre entier. Veuillez modifier cette durée.\n\n\tDurée :" \
                                                  "The release scenario is invalid. The duration value (specified in seconds) under/overflows the memory capacity of an integer number. Please modify this duration.\n\n\tDuration :" }
   set Error(ReleaseRate)                       { "Le scénario d'émission est invalide. Le taux de relâchement (spécifiée en unité/h) doit être un nombre réel supérieur ou égal à 0. Veuillez modifier ce taux de relâchement.\n\n\tTaux de relâchement :" \
                                                  "The release scenario is invalid. The release rate (specified in unit/h) must be a float number greater or equal to 0. Please modify this release rate.\n\n\tRelease rate :" }
   set Error(ReleaseRateRange)                  { "Le scénario d'émission est invalide. La valeur du taux de relâchement (spécifiée en unité/h) déborde la capacité mémoire d'un nombre réel. Veuillez modifier ce taux de relâchement.\n\n\tTaux de relâchement :" \
                                                  "The release scenario is invalid. The release rate value (specified in unit/h) under/overflows the memory capacity of a float number. Please modify this release rate.\n\n\tRelease rate :" }
   set Error(Scenario)                          { "Le scénario d'émission est invalide. Veuillez remplir les champs appropriés vides." \
                                                  "The release scenario is invalid. Please fill up the appropriate empty fields." }
   set Error(ScenarioName)                      { "Le scénario d'émission est invalide. Aucun nom de scénario n'est spécifié. Veuillez spécifier un nom de scénario valide." \
                                                  "The release scenario is invalid. No scenario name has been specified. Please specify a valid scenario name." }
   set Error(ScenarioDirectory)                 { "Aucun scénario d'émission n'existe." \
                                                  "No emission scenario exists." }
   set Error(ScenarioNotAvailable)              { "Le scénario sélectionné est inexistant ou inaccessible." \
                                                  "The selected emission scenario is either inexistant or unaccessible." }
   set Error(NbIsotopes)                        { "Le scénario d'émission est invalide. Aucun isotope radioactif n'est spécifié. Veuillez sélectionner au moins un isotope." \
                                                  "The release scenario is invalid. No radionuclide has been specified. Please select at least one radionuclide." }
   set Error(TotalQuantityAccident)             { "Le scénario d'émission est invalide. La quantité totale de matière relâchée associée à cet isotope est nulle. Veuillez spécifier un taux de relâchement positif pour la durée de cette période d'émission ou supprimer cet isotope du tableau.\n\n" \
                                                  "The release scenario is invalid. The total released quantity of matter associated to this radionuclide is null. Please specify a positive release rate for this release period duration or delete this radionuclide from the table.\n\n" }
   set Error(TotalQuantity2)                    { "\tNom de l'isotope :" "\tName of radionuclide :" }
   set Error(TotalQuantity3)                    { "\tDurée d'émission :" "\tRelease duration     :" }
   set Error(TotalQuantityVirus)                { "Le scénario d'émission est invalide. La quantité totale de matière relâchée associée au virus est nulle. Veuillez spécifier un taux de relâchement positif pour la durée de cette période d'émission.\n\n" \
                                                  "The release scenario is invalid. The total released quantity of matter associated to this virus is null. Please specify a positive release rate for this release period duration.\n\n" }
   set Error(EffectiveSimDuration)              { "La durée effective de simulation doit être supérieure ou égale au pas de temps de sortie des données. Veuillez modifier la durée de simulation ou le pas de temps de sortie.\n\n" \
                                                  "The effective simulation duration must be greater or equal to data output time step. Please modify the simulation duration or the output time step.\n\n" }
   set Error(EffectiveSimDuration2)             { "\tDurée de simulation           :" "\tSimulation duration           :" }
   set Error(EffectiveSimDuration3)             { "\tDurée effective de simulation :" "\tEffective simulation duration :" }
   set Error(EffectiveSimDuration4)             { "\tPas de temps de sortie        :" "\tOutput time step              :" }

   set Error(Intensity)                         { "L'intensité spécifiée n'est pas valide." "Invalid specified intensity." }
   set Error(NbIso)                             { "Il n'y a aucun polluant selectionné." "No pollutant selected." }
   set Error(Elev)                              { "L'élévation n'est pas valide et dépasse la limite de 30000 mètres." "Invalid elevation. It is over the 30000 meters limit." }
   set Error(Previous)                          { "Les résultats d'une simulation précédente sont manquants. Les calculs de concentrations, dépôts et doses seront donc invalides." "Previous simulation results missing. The concentrations, deposition and dosage calculations will be wrong." }
   set Error(MetDBase)                          { "Les répertoires des bases de données météorologiques diagnostiques et pronostiques ne sont pas définis. Veuillez spécifier les répertoires de ces bases de données." \
                                                  "The directories for diagnostic and prognostic meteorological databases are undefined. Please specify the path for these databases." }
   set Error(MetFiles)                          { "Le nombre de fichiers disponibles dans la base de données météorologique localisée sur l'hôte sélectionné est insuffisant pour exécuter le modèle à partir de la date et du temps d'émission de l'accident. Veuillez modifier la date et/ou le temps d'émission de l'accident ou l'hôte." \
                                                  "The number of available files in the meteorological database located on the selected host is not enough to run the model according to accident release date-time. Please modify the accident release date-time or the host." }
   set Error(DateTimeEmission)                  { "\tDate/Temps de l'émission :" "\tRelease date-time         :" }
   set Error(FirstMetDateTime)                  { "\tPremier temps disponible :" "\tFirst available date-time :" }
   set Error(LastMetDateTime)                   { "\tDernier temps disponible :" "\tLast available date-time  :" }
   set Error(LullAtStart)         { "Le scénario d'émission est invalide puisqu'il débute avec une ou plusieurs périodes d'accalmie. Veuillez modifier les intervals suivants : " \
                                    "The emission scenario is invalid because it begins with a lull period. Changes are required for the following intervals : " }
   set Error(LullAtEnd)           { "Le scénario d'émission est invalide puisqu'il se termine avec une ou plusieurs périodes d'accalmie. Veuillez modifier les intervals suivant : " \
                                    "The emission scenario is invalid because it ends with one or more lull periods. Changes are required for the following intervals : " }
   set Error(NoEmission)          { "Le scénario d'émission est invalide puisqu'il ne contient aucune période d'émission. Veuillez ajouter au moins une période d'émission." \
                                    "The emission scenario is invalid because there's no emission at all. Please add at least one emission to this scenario." }
   set Error(Duration_lt_Zero)    { "Aucune durée ne pourra être acceptée pour cet intervalle puisque la durée combinée des intervalles déjà présents est égale ou supérieure à la durée de la simulation." \
                                    "No duration will be accepted for this interval because the sum of all preceding intervals is either greater or equal to simulation duration." }
   set Error(Emdur)               { "Durée de l'émission    :" "Emission duration   :" }
   set Error(Simdur)              { "Durée de la simulation :" "Simulation duration :" }
   set Error(NoRestartFile)       { "Le fichier restart est soit introuvable, soit invalide." "Restart file is either invalid or could not be found." }

   set Warning(MetLocal)          { "La météo n'est pas disponible sur l'hôte d'exécution alors elle sera interpolée localement, ceci peut prendre quelques minutes. Voulez-vous continuer ?" \
                                    "Meteorological data is not available on the running host, it will be interpolated locally, this can take a few minutes, do you want to continue ?" }
   set Warning(SimDuration4)      { "La durée de simulation est supérieure à 72 hrs et le pas de temps de sortie des données est inférieur (ou égal) à 60 min. Ces paramètres d'entrée produiront des fichiers de résultats de grande taille.\n\nVoulez-vous modifier ces paramètres?" \
                                    "The simulation duration is greater than 72 hrs and the data output time step is lower than (or equal to) 60 min. These input parameters will generate large results file size.\n\nWould you like to modify these parameters?" }
   set Warning(SimDuration5)      { "\tDurée de simulation    :" "\tSimulation duration :" }
   set Warning(SimDuration6)      { "\tPas de temps de sortie :" "\tOutput time step    :" }
   set Warning(SimDuration7)      { "La durée de simulation est supérieure à 72 hrs et l'intervalle de temps entre les fichiers de données météorologiques est inférieur à 3 hrs. Ces paramètres d'entrée produiront beaucoup de fichiers de données météo pour nourrir le modèle de dispersion. Ceci nécessitera alors beaucoup d'espace disque.\n\nVoulez-vous modifier ces paramètres?" \
                                    "The simulation duration is greater than 72 hrs and the time interval between meteorological data files is lower than 3 hrs. These input parameters will generate many meteorological data files to feed the dispersion model. Thus, this will require large disk space.\n\nWould you like to modify these parameters?" }
   set Warning(SimDuration8)      { "\tDurée de simulation     :" "\tSimulation duration :" }
   set Warning(SimDuration9)      { "\tInterval de temps météo :" "\tMeteo time interval :" }
   set Warning(OutCV1)   { "Le premier niveau vertical dans la liste n'est pas égal à 0 m. Cette liste sera réinitialisée afin de considérer les particules dans la couche de surface non-résolue." \
                                    "The first vertical level in the list is not equal to 0 m. This list will be re-initialized in order to take into account particles within the unresolved surface layer." }
   set Warning(OutCV2)   { "\tAnciens niveaux verticaux  :" "\tOld vertical levels :" }
   set Warning(OutCV3)   { "\tNouveaux niveaux verticaux :" "\tNew vertical levels :" }

   set Warning(Overwrite)         { "Ce scénario d'émission existe déjà. Voulez-vous vraiment le remplacer?\n\n\tNom du scénario :" \
                                    "This emission scenario already exists. Do you realy wish to overwrite it?\n\n\tScenario Name :" }
   set Warning(Save)              { "Des changements ont été apportés au scénario d'émission.\nVoulez-vous sauvegarder les changements apportés à ce scénario d'émission?\n\n\tNom du scénario : " \
                                    "Changes to the emission scenario has been made.\nDo you wish to save changes to this emission scenario?\n\n\tScenario Name : " }
   set Warning(OverwriteDefault)  { "Il n'est pas permis de remplacer le scenario d'émission par défaut. Veuillez spécifier un nom de scénario différent." \
                                    "Overwriting the default emission scenario is prohibited. Please specify a different scenario name." }
   set Warning(DeleteScenario)    { "Voulez-vous supprimer ce scénario d'émission?\n\n\tNom du scénario :" \
                                    "Do you wish to delete this emission scenario?\n\n\tScenario Name :" }
   set Warning(DeleteDefault)     { "Il n'est pas permis de supprimer le scenario d'émission par défaut." \
                                    "Deleting the default emission scenario is prohibited." }
   set Warning(HalfLife)          { "L'isotope sélectionné a une demi-vie radioactive inférieure à 15 minutes. Cette simulation ne produira pas de résultats pertinents et intéressants. Par conséquent, veuillez sélectionner un isotope différent de" \
                                    "The selected radionclide has a radioactive half-life less than 15 minutes. This simulation will not generate relevant and interesting results. Therefore, please select a radionuclide different than" }
   set Warning(DiskSpace)         { "L'espace disque disponible dans le répertoire de l'expérience est inférieur à la valeur critique d'espace disque.\n\nVoulez-vous tout de même exécuter le modèle ?" \
                                    "The available disk space ino the  experiment diretory is less than the critical disk space value.\n\nDo you still wish to execute the model ?" }

   set Warning(NP_gt_Max)         { "Le nombre de particules total est supérieur au nombre maximum permis. Voulez-vous appliquer le changement suivant au nombre de particule de cet interval pour rectifier la situation ?" \
                                    "The total number of particles is greater than the maximum number of particles. Do you want to apply the following change to the number of particles of this interval to correct this situation ?" }
   set Warning(Emdur_gt_Simdur)   { "La durée totale d'émission est supérieure à la durée de la simulation. Voulez-vous appliquer le changement suivant à l'interval d'émission pour rectifier la situation ?" \
                                    "The total duration of emission is greater than the total simulation duration. Do you want to apply the following change to the emission interval to correct this situation ?" }
   set Warning(SwitchEm2LullA)    { "La totalité des taux de relâchement des isotopes de cet interval étant nuls, cet interval sera considéré comme une période d'accalmie. Voulez-vous continuer ?" \
                                    "All release rates for this time interval are null. Therefore, this interval will be considered as a lull period. Do you want to continue ?" }
   set Warning(SwitchEm2LullV)    { "Le taux de relâchement de cet interval étant nul, cet interval sera considéré comme une période d'accalmie. Voulez-vous continuer ?" \
                                    "The release rate for this time interval is null. Therefore, this interval will be considered as a lull period. Do you want to continue ?" }

}
